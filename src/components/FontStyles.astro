---
import { generateFontCSS, isFontConfigEnabled } from '../utils/font-config';

// 生成字体CSS
const fontCSS = generateFontCSS();
const fontEnabled = isFontConfigEnabled();
---

{fontEnabled && (
  <style is:global set:html={fontCSS}></style>
)}